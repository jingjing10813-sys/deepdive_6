const numbers = [3, 17, 21, 30, 9];
let result = 0;

for (let i = 0; i < numbers.length; i++) {
  result += numbers[i];
}

console.log(`${numbers}의 합은 총 ${result}입니다.`);

const str = "Hello World";
console.log(str.length);